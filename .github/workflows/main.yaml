name: main
on: [push]
jobs:
    check-bats-version:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/setup-node@v2
              with:
                  node-version: '14'
                  working-directory: enotes
            - run: ls -la
            - run: npm install
